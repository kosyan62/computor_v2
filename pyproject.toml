[project]
name = "computor-v2"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "coverage",
    "matplotlib>=3.10.8",
    "ply",
    "pyqt5>=5.15.11",
    "pytest",
]

[tool.ruff]
exclude = [
    "parsetab.py",
]
